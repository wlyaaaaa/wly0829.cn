# 2026-09-09 全站增量与界面验收

保留 33 个独立项目、200 个模块、31 项 Skills，项目卡连续显示 1–33。活动规则为 E126。全源提交复核于 2026-09-09 06:50 UTC，33 项均与已验收来源一致；随后合入轻量工具清单与 E126，07:27 UTC 由既有生成器核对其活动身份。来源、运行和历史测试分别标时。

本轮更新 15 个项目及直接相关的 System、规则和能力说明，其他 18 个项目内容保持原样。电脑 MCP 有独立使用与恢复模块；Chat 自己的用量与 Work/Codex 共享用量、工具是否实际开放分别说明。理解库与表达库既支持正常使用，也能在确有疑点时提供必要只读背景，不能认证身份或写入待验证的新基准。外部研究、文档/演示、图像与数据能力纳入通用能力层，轻量工具按实际用途保留与备份，不为它们扩建项目。

AI 工作区备份与恢复统一展示 Codex、Gemini、Claude、OpenClaw，保留四套独立源码、范围、恢复入口与不同日期的恢复点。Key 采用 VAULT03 与独立密码，和最高权限体系分开保管不同部分材料；不混成授权文件加密，不声称新密钥算法。媒体项目已建立 GitHub 私有仓库，原件字节仍独立保全；Git 总数 49，其中 25 公开、24 私有，45 本地与 4 仅远端，源基线 48 与本轮新增差异分别说明。

## 文字与布局

在既有样式中统一 System 分栏、项目小节、Rule/Skill 栏目标签的深绿层级，正文保持深灰，状态沿用绿/琥珀/灰和少量底色。新增颜色最低对比度 4.91:1；主标题、背景细线、直接模块导航和信息密度保留。

大图弹窗使用固定可用视口与内部滚动，去掉手机横图依赖图片高度的特殊布局。Chrome 实际切图、100–200%缩放、适屏与关闭可用；放大后的图片只在内部滚动区域展示。手机整体宽度异常曾定位为图片收藏扩展的 PINBOX_SEARCHBUTTON 悬浮元素，非站点弹窗越界；没有修改用户扩展。

## 画廊与源文件

个人媒体的既有十张照片及其他画廊优化保留：外部缩略图、选择当前图片才加载高清图，默认不预取相邻大图。上批十图97,372,724→6,555,254字节的有损优化及原件保留结论仍有效，不宣称无损。

Emerald Veil 更新到 0.1.27：电脑各屏、锁屏与泡泡共用当前雨林黑猫底图。手机现用1440×3200 PNG已完整备份到源仓库 main 5a2f8d7，远端原文件回读与输入相同。网页画廊使用两张精确找到的生成原图，全部RGBA解码像素与各自PNG相同：

- 电脑1672×941：原PNG 2,490,116字节，无损WebP 1,743,642字节；缩略图720×405、56,066字节。
- 手机841×1870：原PNG 3,138,778字节，无损WebP 2,353,694字节；缩略图324×720、96,418字节。

两张网页大图合计4,097,336字节。手机原图通过归档生成记录及超分输入链核对，未从超分成品反向缩图。网页不加载17MB/12MB的超分成品；源项目电脑4K图、手机现用PNG及用户原件均保留。新旧原图、网页显示文件与实际壁纸安装证明分开。

## 验收证据

独立 Astra Max 完成源优先完整性、人话与反需求膨胀审查；所有报告的内容差异已经修正。来源项目测试、已安装状态、真实设备与公开发布不相互冒充。OpenClaw 曾发生两次10秒RPC超时，随后同一进程自行恢复但仍响应偏慢；未把它写成已经根治。

本地生成270份完整静态路由、272份HTML和366条搜索记录，公开门扫描650文件、0问题。最后完整219项测试通过，0失败，耗时20.62秒；最终提交的CI仍须重新运行。JavaScript（交互脚本）约14.3KB、CSS（样式）约23.3KB gzip，维持14/23KiB审查线；新增模块和八张能力卡后的搜索索引使用151/175/22KiB的最小实测阈值，不删除专业正文或转成点击后加载。

Chrome 桌面1440×1000、手机390×844检查首页、项目、MCP、AI工作区备份、规则、能力与两壁纸；站点内容无横向溢出，01–33连续，键盘入口可见，图片关闭后焦点返回入口。当前本地预览保留给用户：http://127.0.0.1:53762/ 。公网部署commit、页面及资源回读由发布步骤单独记录，不能用本地验收替代。

## 发布前最后增量

E126 已核对为活动规则，新增本机轻量工具清单的稳定路由。系统与 .agents 技术说明补入按用途查找、探针和指定轨迹分析、入口存在与实际执行的区分，以及选定内容的 G 盘备份；不增加独立项目、Skill 或服务。

按本人最新界面反馈，通用能力拆成 8 张直接阅读卡：理解与研究、图像创作、文档制作、数据分析、代码与工具、浏览器操作、电脑操作、任务协作。卡片无跳转、展开和底部操作栏；搜索仍定位到本卡，并为固定导航留出定位空间。Chrome 现场核对1440px四列、1100px两列、390px单列，8卡均无操作控件，无横向溢出、无页面错误；其它系统分组复用原布局。浏览器常规尺寸接口此次未改变实际窗口，使用同一受管标签页的CDP临时尺寸覆盖完成中屏与手机验收，完成后清除覆盖并关闭该验收标签。
