# 2026-09-07 增量候选验收

## 交付状态

现有 23 个项目的增量候选已完成：15 项实质更新、8 项保留。五份规则更新到 E118，29 项已展示能力及相关 System 内容同步；本项目语义工作默认 Astra Max。候选基于已发布 `bd1b6ae`，位于 `codex/astra-incremental-refresh-20260907`。本轮目标为可发布候选，未推送网站 main 或部署 Pages。

34 个保留项目继续使用原 1–35 槽位，15 留空。本批没有纳入独立建设分支的新项目。来源总账为 48 个仓库（26 公开、22 私有），45 个已验证本地副本、3 个仅远端；四项明确排除与一个本地媒体项目分开处理，System 实际保留 45 项资产，已删除的 Scripts 不再占位。

## 实质更新与必要修复

- `.agents`、Rules、Skills：E118 五文件与来源字节核验；四档原生路由与未分类型号、当前用户选择、局部 Owner CAS、v2 封装、七输入发布、按需读取、个人理解库、原件/录音定位、微信读取包和官方浏览恢复。
- PCConfig、GitHub 总索引、PC Panel Hub、DevConfig Backup、OpenClawGateway、LocalOCR：依据现行来源合入产品/技术变化及有日期的运行结果，保留旧测试和实机证据的原时点。
- ChineseASR、WeChatDirect、个人材料、文书、工作支持与交付、个人理解库、个人媒体、个人健康：原位合入来源变化。个人理解库新增一个有明确来源的模块，原五模块及推荐能力保留。
- TimeAudit、Sunshine、AI CLI Profile Manager、video-scaffold、vault-tool、CACB、Codex Remote 保持原样。学习没有新的明确公开方法制品，保留已验页面，不展开混合私人学习材料。
- 快照收集器与验证器不再把“规则五文件相同、干净且已同步的后续 Skill 提交”误报为规则候选；正确读取 E release v2 的七输入状态。新增五个隔离回归，同一反例下旧 `bd1b6ae` 实现失败、当前实现通过。
- LocalOCR 源 `376a777`：批处理入口统一现有 PowerShell 7.3+，24/24 回归和真实 batch→pwsh→WSL 替身中文空格传参通过，未运行识别模型或 GPU。
- GitHub 总索引源 `2bad541`：两处旧三次 fetch 说明及直接相关断言与真实有界重试对齐，源推送回读完成。
- PC Panel Hub 源 `f883b7d`：两处窗口保护概述与真实启动时序对齐；只改说明，未改变设备状态。
- 媒体 README 的 Packages 范围与当前 PCConfig 对齐；验收记录仅更新文档承诺，原 9 月 5 日代码、测试、目录、手机/云证据不变。
- 无任何现行引用、数据停在旧基线且与 owning 页面重复的首页源包已退役，当前专业内容仍由对应页面和真实来源拥有。

## 验证

| 检查 | 结果 |
| --- | --- |
| 全站测试 | 159/159 通过，0 跳过 |
| 生产构建 | 205 个完整静态页面，308 条紧凑检索记录 |
| 快照绑定 | E118、五份规则、29 项能力，0 个发现项 |
| 公开内容检查 | 来源与生产制品均通过，0 个发现项 |
| 独立 Astra 来源优先审查 | 15 个变动项目与 Rules/Skills/System 通过；9 项具体冲突及日期残留已回读关闭 |
| 反需求膨胀 | 通过；设计创意保持建议，不成为本批施工或发布前置条件 |
| 桌面 Chrome | 首页、目录、新模块、规则工作台、三层阅读可达；阅读层和规则方向键切换有效 |
| 手机尺寸 | 390×844 目录/搜索、320×844 内容页可用，无横向溢出；原窗口尺寸已恢复 |
| 搜索 | 新项目名可达；项目内“可靠回写”结果均留在该项目；既有微信自然问题仍进入原拥有模块 |
| 控制台 | 应用主上下文未见错误；原会话一条日志已定位为浏览器扩展隔离上下文，干净页面会话无警告/错误 |
| 直接路由 | 新模块、Rules、Skills、自定义 404 文档均实际返回内容 |

共享检索实测 124,051 gzip 字节（约 121.1 KiB），使用最小整数 122 KiB 复核线；全项目检索 142,272 字节仍在原 144 KiB 内，最大项目分片 20,083 字节仍在原 21 KiB 内。共享 JS/CSS 仍在原 12/21 KiB 内。未增加浏览器运行时、依赖、后台服务或点击时加载，也未删减所需内容来压体积。

可复现截图留在被忽略的 `docs/design/qa/astra-refresh-20260907/`。截图只证明捕获到的界面与操作，不替代来源、实机或公网部署证据。

## 保留的真实边界

TURZX 发送链停止与过期 fatal 心跳、Telegram 恢复中、手机 163 项待连接清理、部分真实设备/恢复/模型调用未复验仍明确披露。E118 全量源仓库回归未重跑。学习没有新公开制品，不补造更新。

网站仍是本地候选，本次没有 Pages 或公网发布完成声明。设计与创意评审另行交付。
