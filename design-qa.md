# 当前内容、设计与发布验收

2026-09-18 UTC；本批以上次发布的 `90c54b70d397b5d6a134f5d7b22f8697e5f7748a` 为比较基线。这里记录当前候选的验收范围，不作为网站更新日志或业务事实来源。

## 语义与范围

33 个项目逐项得到结论：26 个有实质更新，7 个保留原快照。Rules、Skills、System 与 authority-supply-facts 各自闭合。学习、个人健康、CACB、Codex Remote 四个 manual_owner_only 页面没有本人本轮点名更新请求，未运行来源收集器、未修改正文；工作支持、Typora 与 WeFlow 接入的产品结论仍成立。七个保留项目的原始 SHA-256 与入场记录一致。

语义差分分别列产品与技术的新增、改变、退役及 Unknown，绑定 owning source 的已发布实现、合同或有日期的现实观察。Git、哈希与脚本只用于定位、取证和验证闭包，没有用提交数量决定改写。来源项目、私人正文、现役备份与其他 Owner 的未提交工作没有被本网页任务接管。

本批主要改变：E144 的私人访问、独立保护与终态 Owner 边界；AI CLI 的原生桌面与持续 OpenAI 子会话；Toolkit 的只读诊断和精确取消；ASR 已知文案对齐与 OCR 生命周期；多层备份、可恢复配置和本机控制窗口；材料和媒体的实际覆盖；文书屏幕彩色与打印灰度分工；Antigravity CLI 的选定轻量备份；Vault 的双槽保全、原件保留和发布回读。各项历史测试、安装、当前运行、真人体验与新机恢复不互相替代。

AI CLI 原页面遗漏了已有桌面接入与后台协作，按内容质量修复处理，补齐原位模块后重读三层说明，不冒称普通数据增量。Rules、Google、Vault、恢复与 System 的跨页矛盾由当前来源纠正；系统搜索恢复到准确的项目身份入口。正文保留必要用法、失败与恢复边界，不以删减内容换取体积通过。

## 阅读与模型证据

本轮由当前 ChatGPT 主任务完成来源对照、编辑和终读。活动 native-economy-routing 已读取；该宿主没有暴露可独立核验的精确 model/effort 或原生子代理接口，因此没有把自报型号冒充宿主证明，也没有称独立 Astra 子代理验收通过。没有改用较低模型、本地模型或启动 Codex 模型任务代做。用户选择的 Astra Max 默认及项目允许 High+ 的规则原样保留；本轮模型身份限制与产品能力分开。

来源真实未知留在各页：新机/物理冷启动、手机实际使用、云端模型调用、未接入介质、数据覆盖、像素与媒体播放分别说明。新源码、新恢复清单或一个绿色任务状态不会抹掉这些边界。

## 信息架构与页面

保留 33 项目、31 个公开 Skills、205 个来源定义模块和既有视觉系统。新增模块仅对应已有能力遗漏与已知文案对齐，没有新增项目、重复刷新卡片、更新时间线或定时任务。System 仍有八项定时协作；网页更新是第 7 项，副驾驶胶囊巡检是第 8 项，两者各读自己的任务与恢复来源。

`app/page.jsx` 与 `app/style.css` 的原始入场 SHA-256 完全不变。原有完整静态 HTML、三层阅读、项目内搜索、自然目录导航和可访问键盘路径继续使用，没有增加点击时叙事加载或新依赖。未改工程文件中的既有混合换行保留，未为规范格式重写。

## 已完成验证

- 最终 `npm run build`、`test:built` 与 PUBLIC 内容门通过：228 项测试通过，0 失败、0 跳过。新回归覆盖电子/打印媒介、Antigravity 选择范围与 Vault 实际回读；旧断言只在对应来源语义改变后调整，没有删除安全与失败边界。
- 当前构建生成 276 份完整静态页面、373 个紧凑搜索记录。公开门核对 235 个源文件、435 个构建文件，零发现。共享交互 JS/CSS 和搜索投影通过原有体积门；完整叙事仍在静态 HTML 中。
- Chrome 在 1440×1000 与 390×844 下完成 38 项浏览检查：受影响代表 Project、Rules、Skills、System、三层阅读、五规则切换、全局和项目内搜索、移动搜索、空结果、键盘操作与自定义 404。页面无横向溢出、无替换乱码、无页面脚本异常。手机规则使用真实可见下拉框，不点击隐藏桌面标签。
- 用户已有外部 Chrome 中实际打开并阅读本地首页和电脑 MCP 模块，另目检手机宽度的首页、电脑 MCP 与 Rules 页面图；原有标签保留，只关闭本任务新开的三个预览标签。手机宽度模拟不冒充每款真实手机或原生客户端实测。
- AI refresh v2 验证覆盖 33 项项目结论、四个全局表面和当前文件字节；语义差分与实际修改范围一致。不可读证据未被猜测补齐，旧日期没有批量改成今天。

机器可读的本轮计划、结果、不可变文件承诺和分层回执保留在忽略的 `.panel-refresh/`，可重建页面图保留在忽略的 `docs/design/qa/current-refresh/`。不将临时过程脚本、原始私人材料或桌面其他应用截图提交到 PUBLIC。

## 发布与收口

按既有授权仅提交本次相关正文、Registry、回归与此验收文件，正常推送现有 PUBLIC main；不合并或清理两个无关历史工作树。确切 commit、Pages 结果和公网首页/代表页面/搜索/移动端回读在发布后的本轮结果中记录，不为给本文补部署号再制造一个网站提交。

源码审查、构建、安装、现实业务、网页发布与用户最终满意是不同层。本次网页发布不会替来源项目运行备份、解密、恢复、网络重置或真实模型调用；没有为通过验收改变电脑服务、磁盘保护或来源项目的所有权。