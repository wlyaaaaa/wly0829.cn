# 第18项目 vault-tool：内容与本地成品验收

## 目标、范围与结论

用户选定 vault-tool 为第18项，明确要求设置目标实施、遵守网站规则、完整产品内容、专业人话，并允许子代理和必要来源修复。现有网站架构保持不变，完成一个总览、八个来源模块和对应 Skill 说明。没有新建在线加密服务、密码中心、容器格式、后台任务或下一项目占位卡。

本地成品与独立内容验收 PASS：来源十轴全部覆盖；独立来源对照与首次读者复验均 P0=0、P1=0。本文记录内容、源码、隔离验证与本地浏览器证据；网站的提交、Pages 部署与公网三方读回是单独的最终交付证据，以本轮完成报告和对应 [Pages 运行](https://github.com/wlyaaaaa/wly0829.cn/actions) 为准，不把本文的未来提交自写为已验证。

其余17个项目的叙事文件没有改写；只更新相关 vault-workflow、System 对应入口／详情数量及必要共享供应事实。现为18项目、5条规则、28项能力。旧工作树与受保护的其他任务均未接管或清理。

## 来源与必要修复

- PUBLIC vault-tool：核心实现提交 `0b9d4c462d7b640baa42ea0d51df5768ea15b972`；最终人工入口更正提交及当前 main 为 `8293ea88775c71d2b5ca2363cc1ba9abe89c81e2`。本地、远端默认分支回读一致，工作区干净。
- PRIVATE Skill 来源：`.agents/main=e67cca11d9ae8ba1bd70e9be34ea16a95e2505f7`，已发布回读。E99 活动规则及五文件规则集未变。
- 只修复当前真实问题：损坏结构的误推荐、同名新版被旧内容覆盖、显式算法静默替换、CLI错误码、菜单中断、原KDF保持、剪贴板测试隔离、本地成员／槽位保真、失败终态与发布字节回读。
- 未增加算法政策层、通用双密码编辑器、全局权限层或恢复服务。删除了一次对相同暂存候选的重复解密，保留写前与最终文件自检。
- 最初旧测试曾尝试写入并清空系统剪贴板，成功与否未记录；未读取其内容。后续测试已隔离，不能把首次基线说成从未发生该尝试。

## 独立来源能力覆盖

| 来源能力轴 | 产品与技术承载 |
|---|---|
| 文件入库、合并、替换与同名内容 | 文件入库 |
| 查看、检索、复制和提取 | 查看与提取 |
| 明文生命周期、中断和清理 | 查看与提取 |
| 密码、密钥文件、KDF、格式和环境 | 密码与格式 |
| 改密、迁移与备份寿命 | 库维护 |
| 双密码内容及容量分桶 | 双密码容器 |
| 图片载体与原样取回 | 图片载体 |
| 严格元数据检查与计划 | AI 与本地入口 |
| 本地查看／编辑与失败恢复 | AI 与本地入口 |
| 私人密文发布及独立README保护 | 密文备份 |

来源审查先建立这十轴，再检查页面，不以页面自己的八模块反证完整。关键入口更正已经复验：交互 decrypt 才有筛选提示，--extract 是全量；CLI明文 stdout 不能被AI捕获；普通 Publish 有默认分支 blob 字节回读，README保护只有本地候选自检与单提交API结果。

## 功能与证据分层

- 来源 Python 回归：106 passed，另有13个子用例通过，0失败／跳过；pytest 21.03秒，外层22.151秒。
- 真实算法虚构样例：Windows CNG，无KDF/AES替身，内部23.994秒／外层24.374秒；覆盖往返、错误口令、改动密文、同名合并、选择性提取、维护、参数保留、双密码和图片载体。
- 本地 Skill：41/41隔离回归，20.551秒；独立 Terra 另建虚构样例并复核，独立 Sol 对失败连续操作的8条复验通过，1.166秒。
- 回退失败序列实际验证：目标替换后自检失败、回退再失败、原文无变化重试及修改重试；均保持失败，不再次写入，保留字节正确的原密文恢复副本。不能承诺此时目标路径已经恢复。
- 发布脚本：九类模拟 gh 用例通过，保留真实脚本入口；不进行真实私人上传。PUT／非404错误／错误字节均不能报成功。
- 当前供应 source/install/transaction 为 pass；canonical junction 指向现行 Skill 源码。已安装入口 LocalEdit -WhatIf 返回 planned，解密／显示／保存标志均为false。没有以此冒充真人操作。

严格保留未知：真实密码框、本人的真实库和密钥文件、私人远端上传、强杀／断电、跨平台与干净新机恢复未在本轮验收。V1目标选择使用结构有效容器和受控旧明文模拟；小KDF参数仅供虚构保留性回归，不能作为真实数据配置建议。

## 公开样例

`public/media/vault-tool/synthetic-roundtrip.json` 是实际虚构验证结果的原样副本：4,231 B，SHA-256=`4add06a29a8b12f68a9f41783bb859a4463c546bc014ec3eaf69940800211c36`。Git属性固定原始字节，不做转码；公开文件没有密码、密钥文件或本人原文。

三份虚构文件合计2,165 B，形成65,536 B容器；内容往返与筛选清理分别验证。独立读者从桌面与手机实际点击下载，保存字节、SHA和JSON解析一致。下载的初次取消来自验收浏览器保存路径设置，修正后正常，未为此改网页。

## 本地构建、搜索与浏览器

- 完整构建：18项目，168个完整静态页面，277条紧凑搜索记录。
- 网页测试：96/96通过，0失败、取消或跳过。
- 快照绑定：E99、当前Skill源码e67cca1、5份规则、28个安装意图和28项能力；正式验证通过。
- PUBLIC内容门：478个文件（178来源、300成品）检查通过，0发现。
- 根浏览器：总览+8模块 × 320/390/768/1440四种宽度，共36组合均200、无横向溢出；三读层切换正常，未见页面脚本错误。禁用JavaScript后总览三层仍全部可读；实际JSON下载保持当前页并取得相同4,231 B/hash。
- 独立读者：1440、375、320宽度，模块、自然搜索、返回、回顶、无结果状态及下载均验证；能独立复述输入、产出、人工决定和失败范围。
- 自然搜索“加密保存”“只找一段文字 取出附件”找到vault-tool；项目内“换电脑打不开”直达密码与格式。测试不锁死其他合理查询的唯一模块排名。
- 速览明确密钥文件是建库时可选绑定；未改变其他项目的共享手机布局。

## 体积与复杂度

发布前基线搜索 gzip 为93,603／118,114 B，最终候选测得97,524／123,274 B，vault单项目分片6,221 B。新增八条模块为八个唯一URL，116个现有模块全部有显式紧凑投影；总览替换旧System入口，不重复挂一个旧条目。

按实际最小整数KiB，搜索门限从92/116调整为96/121；它们是实测反膨胀阈值，不是删正文的额度。12 KiB共享JS、21 KiB CSS、20 KiB单项目分片不变。共享JS仍35,257 raw／11,596 gzip，CSS仍129,197 raw／21,082 gzip，均与基线字节一致；没有新依赖、压缩协议、点击时加载或后台服务。

## 发布与收口范围

当前内容观察时刻：2026-09-03T09:42:05Z。正常发布目标仅为既有PUBLIC main和Pages；还需在最终交付中使本地HEAD、远端main、Pages运行头完全一致，并实际读回总览、八模块、Skill、自然搜索和样例字节。公网结果不由本地测试代替。

来源面板影响已评估，相关变化由当前同一活跃第18项目目标吸纳，没有创建重复网站任务。来源各精确scope已分别收口；网站scope在实际发布回读完成后释放。临时预览和验收浏览器只供本轮使用，结束后停止；不删除旧工作树或别的任务数据。
