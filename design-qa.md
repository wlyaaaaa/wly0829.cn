# 2026-09-07 本地候选与界面验收

## 交付状态

本地候选包含 29 个完整项目：原有 23 项完成 15 项实质更新、8 项保留，另外吸收 6 个 Gemini 已交付项目并完成独立源优先验收和必要修复。五份规则为 E118，29 项已展示能力及直接相关 System 内容同步。候选基于 `bd1b6ae`，位于 `codex/astra-incremental-refresh-20260907`。用户选择先看本地实际版本，网站 main 与 Pages 未发布本批改动。

保留 34 个独立项目，继续使用原 1–35 价值槽位，15 留空；6 个新项目按原固定名次进入候选，没有整体覆盖旧工作树。来源总账仍是其观察时点的 48 个仓库（26 公开、22 私有），45 个已验证本地副本、3 个仅远端；System 复用既有资产身份，已删除的 Scripts 不再占位。WeChat History AI Bridge 尚未由 Gemini 交付，未提前建卡。

## 实质更新与必要修复

- `.agents`、Rules、Skills：E118 五文件与来源字节核验；四档原生路由与未分类型号、当前用户选择、局部 Owner CAS、v2 封装、七输入发布、按需读取、个人理解库、原件/录音定位、微信读取包和官方浏览恢复。
- PCConfig、GitHub 总索引、PC Panel Hub、DevConfig Backup、OpenClawGateway、LocalOCR：依据现行来源合入产品/技术变化及有日期的运行结果，保留旧测试和实机证据的原时点。
- ChineseASR、WeChatDirect、个人材料、文书、工作支持与交付、个人理解库、个人媒体、个人健康：原位合入来源变化。个人理解库新增一个有明确来源的模块，原五模块及推荐能力保留。
- TimeAudit、Sunshine、AI CLI Profile Manager、video-scaffold、vault-tool、CACB、Codex Remote 保持原样。学习没有新的明确公开方法制品，保留已验页面，不展开混合私人学习材料。
- 快照收集器与验证器不再把“规则五文件相同、干净且已同步的后续 Skill 提交”误报为规则候选；正确读取 E release v2 的七输入状态。新增五个隔离回归，同一反例下旧 `bd1b6ae` 实现失败、当前实现通过。
- LocalOCR 源 `376a777`：批处理入口统一现有 PowerShell 7.3+，24/24 回归和真实 batch→pwsh→WSL 替身中文空格传参通过，未运行识别模型或 GPU。
- GitHub 总索引源 `2bad541`：两处旧三次 fetch 说明及直接相关断言与真实有界重试对齐，源推送回读完成。
- PC Panel Hub 源 `f883b7d`：两处窗口保护概述与真实启动时序对齐；只改说明，未改变设备状态。
- 媒体 README 的 Packages 范围与当前 PCConfig 对齐；验收记录仅更新文档承诺，原 9 月 5 日代码、测试、目录、手机/云证据不变。
- 无任何现行引用、数据停在旧基线且与 owning 页面重复的首页源包已退役，当前专业内容仍由对应页面和真实来源拥有。
- 后续实机修复：TURZX `a4d8e22` 恢复持续送帧，同一隐藏启动器接住守护进程异常退出；实测约 63 秒恢复，主动停止不复活，本人确认实体屏正常刷新。PCConfig `6fdaf00` 完成单应用 RTSS 排除，未宣称所有原生崩溃永久消失。
- 6 个新项目：ProxyClean、Codex Memory Backup、MeshClip Kit、LLM Backend Toolkit、Typora Theme Pack、Codex App Power User Playbook。源修复分别覆盖物理出口判断、备份来源说明、零设备误报、准确的责任/续问说明与真实 PDF 入口；源码、文档、机器状态及外部设备未知分别陈述。
- 界面：首页搜索与常用入口前置，系统背景介绍可选读；项目卡片数据、状态、边界直接显示，删除额外展开与重复结果行；图片一次打开既有大图查看器。背景细线保持原样。
- AI 续作说明：在当前项目或模块中填写目标，生成包含公开背景、来源、日期与未知项的文本；真实浏览器复制再粘贴核对 6023 字符完全一致，不调用模型或创建任务。

## 验证

| 检查 | 结果 |
| --- | --- |
| 全站测试 | 198/198 通过，0 跳过 |
| 生产构建 | 242 个完整静态页面，339 条紧凑检索记录 |
| 快照绑定 | E118、五份规则、29 项能力，0 个发现项 |
| 公开内容检查 | 来源与生产制品均通过，0 个发现项 |
| 独立 Astra 来源优先审查 | 原有变动审查保留；6 个新项目各自源与两层内容通过，12 条最终页面回读与构建文件一致 |
| 反需求膨胀与界面审查 | 通过；按用户更正删额外点击、保留必要直接信息，不为文案或测试虚构项目能力 |
| 桌面 Chrome | 首页、目录、新模块、规则工作台、三层阅读可达；阅读层和规则方向键切换有效 |
| 手机尺寸 | 390×844 目录/搜索、320×844 内容页可用，无横向溢出；原窗口尺寸已恢复 |
| 搜索 | 新项目名可达；修正跨分词伪词造成的相关性回归，照片请求到媒体、文档纠正到材料；既有专业命令与项目作用域保持 |
| 控制台 | 应用主上下文未见错误；原会话一条日志已定位为浏览器扩展隔离上下文，干净页面会话无警告/错误 |
| 直接路由 | 新模块、Rules、Skills、自定义 404 文档均实际返回内容 |

最终实测 gzip 字节：共享 JS 13,923、CSS 22,932、共享检索 138,669、全项目检索 159,042；分别使用独立复核认可的最小整数 14/23/136/156 KiB 线。最大项目分片 20,083 字节仍在原 21 KiB 内。新增 6 个完整项目及实际交接功能有明确消费者，未增加浏览器运行时、依赖、后台服务或点击时加载，也未删减所需内容来压体积。

可复现截图留在被忽略的 `docs/design/qa/astra-refresh-20260907/`。截图只证明捕获到的界面与操作，不替代来源、实机或公网部署证据。

## 保留的真实边界

当前本地候选有 29 个完整项目，仍有 5 个尚未交付的规划项；下一项为 #28 WeChat History AI Bridge。Gemini 的后续提示词不等待界面、验收或发布串行结束；不同施工支路并行，最后统一集成。

TURZX 已恢复并获得本人现场确认；HS2 当前叠加层未运行。MeshClip 当前没有可用 KDE 对端，H 冷盘离线；Telegram 恢复中、手机 163 项待连接清理、部分真实设备/恢复/模型调用未复验仍明确披露。E118 全量源仓库回归未重跑。学习没有新公开制品，不补造更新。

用户已选择直接实施本地界面和续作说明功能，并明确保留背景细线原样。界面、内容更新、Gemini 成果验收与源项目自动修复并行推进，网站暂不发布。

网站仍是本地候选，本次没有 Pages 或公网发布完成声明。用户可随时访问预览提供阶段意见，不阻塞已授权的后续工作。
