# wly0829.cn 当前设计验收

## 验收对象

本文件只记录当前实现，不累积旧版设计过程。验收范围包括项目首页、`.agents`
总览与六个模块、规则工作台、Skills 目录与详情页，以及全局导航、搜索、移动端
菜单和动态背景。

本轮证据时间：2026-08-29 14:38（中国时间）。所有下列截图均在最终源码、14:19
动态快照和最新 HMR 状态下重新生成；没有复用此前版本的截图结论。

网站首先是吴乐阳本人使用的只读面板。判断界面是否合格，不看它像不像营销页，
而看本人能否在有限屏幕里快速找到项目、规则和能力，并继续读到足够完整的细节。

## 已确定的视觉方向

- 白色为主背景，纯绿色只承担状态、选择和少量结构强调。
- 页头保持单行：姓名、全站搜索、项目 / 规则 / Skills，以及外部入口。
- 桌面项目目录使用两列；项目扩展后，第一屏应能容纳四张真实卡片。移动端一列，
  第一屏约能看到两张卡片。
- 卡片整体可点击，公开性或私有性放在右上角，不设置重复的“进入项目”按钮。
- 项目、规则和 Skills 目录直接进入内容，不重复堆放大标题和空泛介绍。
- 全页面背景使用固定的缓慢平行曲线；正文滚动不会重启动画，也不会让曲线在区块
  交界处断裂。
- 不使用粒子、尖峰、鼠标吸附凹陷、发光、渐变玻璃或伪造的项目预览图。
- 图标使用维护中的品牌与界面图标库，不手绘近似标志。

## 当前信息架构

顶层导航只有三个入口：项目、规则、Skills。`.agents` 固定为第一个项目；规则页
在同一页面内切换五份当前规则，不创建五条详情路由；Skills 目录按实际使用价值
排列，每项都有独立详情页。网站工程自身不作为项目卡出现。

全站搜索位于页头，可检索项目、模块、规则和 Skills。搜索结果必须说明命中的内容
类型和位置，不能只返回一串没有上下文的标题。

## 交互验收标准

- 页头在桌面和移动端都不能产生水平溢出。
- 移动端菜单支持按钮开关、点击页面关闭和 `Escape` 关闭。
- 项目卡、搜索结果、规则选择和所有外部链接都可用键盘访问。
- 规则工作台支持鼠标、键盘方向键和移动端原生选择器；切换规则不改变路由。
- 项目模块导航在长页面中保持可见，并在内容超出时显示真实滚动条；当前选项不会
  因页面滚动而丢失。
- 动态背景属于全页面固定层；内容滚动与动画时间彼此独立。
- 系统要求减少动态效果时，背景只渲染静态帧。

## 可复现证据

浏览器截图保存在被忽略的 `docs/design/qa/`，不进入公开提交历史。最终验收至少保留：

- `home-1440x900.png`：桌面项目首页。
- `home-390x844.png`：移动端项目首页。
- `rules-1440x900.png`：桌面规则工作台。
- `rules-390x844.png`：移动端规则工作台。
- `skills-1440x900.png`：Skills 目录。
- `skills-390x844.png`：移动端 Skills 目录。
- `skill-project-entry-1440x900.png`：代表性能力详情页。
- `skill-detail-390x844.png`：移动端能力详情页。
- `search-1440x900.png`：全站搜索“刷新看板”的唯一命中结果。
- `project-scroll-nav-1440x900.png`：长页面滚动后的固定模块导航。
- `mobile-menu-390x844.png`：移动端菜单展开状态。
- `background-a-1440x900.png` 与 `background-b-1440x900.png`：同一位置不同时刻的
  背景帧，用于确认缓慢运动真实存在。

每轮最终验收同时检查控制台错误、直接路由、404、键盘操作、桌面与移动端溢出，
以及生产构建中的公开内容门。截图本身不能替代这些检查。

## 当前结论

本轮使用当前源码、当前生成快照和本机开发服务器重新验收：

- 1440 x 900 桌面首页、规则、Skills 和能力详情页均无横向溢出。
- 390 x 844 移动端首页和项目模块无横向溢出；页头保持单行，高约 65 px。
- 移动端菜单可展开；点击页面空白会关闭，`Escape` 也会关闭并把焦点还给菜单按钮。
- 全站搜索“刷新看板”返回一条有类型、标题和完整意义说明的结果；进入后可直接看到
  当前 saved-project 缺失造成的真实阻塞，没有把负向验证冒充正向 E2E。
- 宽泛搜索会报告完整命中数并明确只展示前 9 项；混合查询“candidate 不能冒充
  active”将重大动作保护排在第一，“同一个目标不要反复问我授权”把授权与委派排在
  第一。
- 规则工作台显示第 79 代、五份闭包、candidate pending 和 production active；
  桌面方向键可从第一份切换到第二份，移动端使用原生下拉选择器。
- 项目模块导航在长页面滚动后仍可见；移动端横向滚动条真实存在，当前第六模块自动
  滚入可见区域。
- 两张相隔约 900 ms 的背景截图 SHA-256 不同，证明动画持续运行；页面滚动没有参与
  动画相位计算。切换 reduced motion（减少动态效果）后，两次 canvas 指纹保持相同。
- Skills 目录明确写成“公开 MVP 收录 22 / 当前供应 24”；新任务创建受阻使用 repair
  状态色，首页的 0 只表示“基础矩阵待闭合”，不再概括所有 Skill 缺口。
- 浏览器控制台 error / warning 为 0。
- 13 项网站合同测试、32 条直接路由、自定义 404、生产构建和公开内容门全部通过。

截图只能证明可见界面。本轮另外用 DOM 结构验证了标题层级、导航、标签、键盘切换和
焦点恢复，但没有声称完整屏幕阅读器或 WCAG 合规；这些属于以后需要专门辅助技术时的
独立验收。

final result: passed
