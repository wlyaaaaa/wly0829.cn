# 本地展示修复验收

本批按2026-09-08的用户反馈完成，等待本人发布指示，尚未推送或部署。发布基线为ec5b87e5233e01af2ddfbbc2212f165b714c001c，其来源截止2026-09-08T08:00:42Z。本批只改布局、可见编号与图片交付，没有重新冻结或刷新来源项目。

当前保留 33 个独立项目，项目卡片连续显示 1–33；内部价值槽位和项目相对顺序保持。33个完整项目、196个模块、31项Skills（能力入口）、活动规则E121均沿用已验收内容。

## 用户反馈对应修改

- 系统资产卡片的标题、正文、动作栏共用自然行高；同一排的分隔线和首个入口同时对齐，不依赖固定高度或补空链接。
- 同一目标链接只显示一次；文书卡保留完整项目、位置未知时找原件、文书能力三个不同入口。
- 九项系统域使用三列，五项使用3+2，尾项不再改成满宽四分区横条。手机按实际宽度换为两列或一列。
- 项目显示编号来自当前展示列表的位置；退役内部槽位不再造成14→16跳号，不新增占位项目。
- 保持已选十张实拍、原构图、方向与色彩配置；外面使用原有720像素缩略图，点开当前一张2560像素长边高清大图。全部画廊默认不额外预取相邻大图。

## 图像优化

十张旧显示JPEG合计97,372,724B，WebP（网页图像格式）显示图6,555,254B，减少93.27%。十张缩略图644,818B且SHA逐张不变。采用长边2560、质量82；已比较质量86与3200长边的样张。树叶、维港楼宇、棕榈、海天渐变和中庭线条的同区域比较显示：压缩图明显清楚于放大缩略图，接近同尺寸源图；更大方案没有相称的可见增益。天空细颗粒有所平滑；这是有损显示副本，不宣称无损或与HDR（高动态范围）原件等价。

其他八组48张大图中，26张PNG改为无损WebP，22张JPG的无损候选更大，因此保留原文件。全部26张均比较完整RGBA像素字节、尺寸、方向及ICC（色彩配置），逐项完全相等；没有用近似质量分数替代像素验真。两张壁纸仍为3840×2160。其他既有39张缩略图1,047,466B，逐项与发布基线完全相同。

其他大图25,234,018→14,328,879B，减少43.22%。全部58张大图合计122,606,742→20,884,133B；加上既有缩略图、移除不再交付的三份HEIC副本后，全部画廊公开文件135,562,937→22,576,417B，减少83.35%。用户原件未触碰；有原件身份的技术说明保留来源身份，另列显示文件身份。下载提供网页显示图。已有48张截图/照片的说明与证据等级保持；LocalOCR首图仅将“逐字节相同”纠正为尺寸与解码像素相同。

## 独立实页验收

Astra Max独立设计与反需求膨胀审查通过，没有增删项目功能或额外操作。外部Chrome实测：

- 1440×1000桌面的电脑域、个人协作域、备份域，每排动作栏上沿和首条入口差值均为0。390×844手机所有卡片单列，页面和逐卡横向溢出均为0。
- 项目33张，编号严格01–33；原截图处现在为13、14、15、16，项目身份与顺序保持。
- 个人媒体十张图逐张查看，长边2560、比例与细节正常。打开前仅10张缩略图请求、0张大图请求；依次切换时只新增当前选中图片请求。
- 手机实测适屏→200%放大→横向平移→恢复适屏→关闭，焦点回到原第4张缩略图按钮，页面恢复滚动。
- TimeAudit文字数据图3840×2146、壁纸图3840×2160实际打开清楚；两页打开前0张大图请求，打开后各只请求选中图片。所检页面控制台警告与错误均为空。

当前可重建QA图片（不入Git）：
- docs/design/qa/post-release-system-personal-desktop.png
- docs/design/qa/post-release-gallery-mobile-zoom.png
- docs/design/qa/post-release-timeaudit-data-image.png
- docs/design/qa/media-display-comparison-20260908.png

## 本地验证

完整构建生成266条完整静态路由、268份HTML和358条搜索记录；冻结快照检查通过，公开内容检查646文件、0问题。网站完整测试首次218/219通过，仅Typora旧PNG文件头断言未跟随实际无损WebP转换；修正该格式断言后对应文件3/3通过，219项所需检查全部完成。另检查268份HTML的680处图片引用，无缺失文件。代码差异检查通过。

共享JavaScript（交互脚本）和CSS（样式）的标准gzip实测14,261/23,221B，现有14/23KiB审查线内，没有新增运行依赖。预览 http://127.0.0.1:53762/ 保留给本人查看。本批没有发布结果，不能把本地通过写成线上已生效。
