# 当前设计与发布候选验收

2026-09-10（北京时间）本轮在已发布 e0c90dd 上做增量，原有信息架构、样式、图片和项目选择不变。用户要求全页面复核、必要自动修复、每次及每周明确汇报变化，然后发布。

保留 33 个独立项目，项目卡片连续显示 1–33；内部价值槽位不作为显示编号。

## 内容结果

- 33 个项目全部有结论：5 个有实质更新、28 个保留；系统、规则、Skills 与活动规则事实同步。仍为200模块、31 Skills、270完整静态路由、272 HTML，搜索367条（167共享＋200项目模块），无相同重复记录。
- E127/ecf2086 已活动，五文件规则集与当前指针核验通过。解释真实目标与可换方案、已有能力修复/正式替换，保留来源、测试、安装与真实使用分层。
- PCConfig 来自 a69d632（含893b46c台账修复）：双向MCP、开机服务与用户桌面、按需维护、断连结果、副机部署/恢复胶囊分开说明。94/94任务定义与运行时台账已对齐；7通过/1历史Ollama失败，同入口后续成功和当前接口可达不抹历史。MCP登录数据尚无登记独立备份，明确写入产品与技术层。
- 学习方法采用 e732192 通用规则：共用背景与教学反馈分开，Astra Max及独立审查按实际需要；不读写私人主题/进度。健康只有私人记录变动，公开方法保持原样。
- AI工作区备份分别显示G点7016文件、活动源7067文件与本轮H不可用；旧H记录只保留为历史。OpenClaw当前RPC/health通过，03:50/03:55Z慢样本、消息未验与当前渠道状态各自保留。
- System第8项为副驾驶恢复胶囊与每周巡检。后台七天周期备份与每周AI只读巡检分开；正常USB未插不报警。网页更新任务每次汇报具体变化，实际时程和状态未改变，没有新增调度任务。
- 独立Astra Max完成源先行内容审查；提出的MCP备份缺口、Health旧合同、慢样本历史限定、通知语义均已原位修正并回读。

## 实际界面

外部Chrome查看生产静态预览。桌面1440/1920与手机390（内容375）实测：目录编号1–33连续；8项定时协作及自动计算的总数一致；全站导航、项目速览/产品/技术和原生模块入口可用。自然词“副驾驶恢复胶囊”搜索到PCConfig并进入副机详情。

手机逐一打开电脑MCP、学习、AI工作区备份、OpenClaw、Rules、两项更新Skill，均clientWidth=scrollWidth=375，控制台无error/warn。原有缩略图/按需大图、8张无链接通用AI能力卡及正文背景细线保持。

真实浏览发现：首次带章节片段进入时，其他静态场景收起会改变页面高度，导致原生落点偏移。现有初始化在新导航完成场景选择后对齐目标；刷新和历史返回保留浏览器位置恢复，不新增滚动监视器。新桌面导航目标约130px、手机122px，均位于固定页头下。

## 验证与大小

- 最终本地测试220/220，0失败（21.78秒）；源PCConfig聚焦验证、57项重建计划与实机回读单独成立。
- 构建通过；公开内容门651个文件、0问题，未调用Codex Security。
- v2增量结果校验：33项目＋4共享面的字节/语义差分闭合。修复旧验证器强制全部候选采集入口必跑的问题；只校验本次实际声明及需要的完整可见性回执，未执行不得填写通过。
- 共享JS gzip14320B、CSS23289B；共享搜索154497B、全项目179888B、最大PCConfig分片23455B。只有真实新内容导致全项目/单项目阈值最小调整为176/23KiB，共享搜索151KiB、JS14KiB、CSS23KiB不变。完整正文仍在静态HTML，没有新增点击时内容加载。

## 发布与保留

按当前用户授权定向提交并正常推送既有PUBLIC main，随后核对GitHub Actions、Pages SHA及公网页面。实际发布回执与本轮具体更新清单存于主任务outputs，避免在源码内追记部署自身的下一次提交。

本地 http://127.0.0.1:53762/ 是本任务可控preview，按用户要求继续保留；临时浏览器尺寸与验收标签结束时清理。用户原件、其他任务、来源并发dirty和EV实验未修改。主机H当前不可用、MCP登录数据独立备份缺口、物理未登录冷启动/新机/NVMe及消息E2E未验均保持明确，不以网站测试代替。
